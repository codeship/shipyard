test:
  build: .
  dockerfile: Dockerfile.ci

dev:
  build: .
  dockerfile: Dockerfile.dev
  command: jekyll serve --watch --incremental --baseurl=''
  ports:
    - 4000:4000
  volumes:
    - .:/styleguide/
